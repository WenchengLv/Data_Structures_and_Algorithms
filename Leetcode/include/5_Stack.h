#pragma once
#include <iostream>
#include <string>
#include <stack>

using namespace std;

class Solution {
public:
    string longestPalindrome(string s) {
        Stack<char> target;
        Stack<char> tmp;
        int index=0;

//        for (int i=0 ; i < s.length() ; i++){
//            target.push(s[i]);
//        }
        for (int i=0 ; i < s.length() ; i++){
            for (int j=i ; j < s.length() ; j++){
                if ()
            }
        }
    }
};